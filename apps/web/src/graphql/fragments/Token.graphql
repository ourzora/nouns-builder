fragment Token on Token {
  tokenId
  name
  description
  image {
    url
    mediaEncoding {
      __typename
      ...ImageMediaEncoding
    }
  }
  owner
  mintInfo {
    mintContext {
      blockTimestamp
    }
  }
}
