query exploreDaosPage(
  $orderBy: Auction_orderBy
  $orderDirection: OrderDirection
  $where: Auction_filter
  $skip: Int
) {
  auctions(
    where: $where
    orderBy: $orderBy
    orderDirection: $orderDirection
    first: 30
    skip: $skip
  ) {
    ...ExploreDao
  }
}
