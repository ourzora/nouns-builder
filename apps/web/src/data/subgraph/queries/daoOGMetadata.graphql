query daoOGMetadata($tokenAddress: ID!) {
  dao(id: $tokenAddress) {
    name
    description
    contractImage
    totalSupply
    ownerCount
    proposalCount
    tokenAddress
    metadataAddress
    auctionAddress
    treasuryAddress
    governorAddress
  }
}
