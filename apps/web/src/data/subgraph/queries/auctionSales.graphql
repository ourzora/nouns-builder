query auctionSales($startTime: BigInt!, $daoId: String!) {
  dao(id: $daoId) {
    auctions(where: { startTime_gt: $startTime }) {
      id
      endTime
      winningBid {
        amount
      }
    }
  }
}
