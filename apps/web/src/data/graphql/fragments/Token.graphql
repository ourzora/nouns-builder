fragment Token on Token {
  tokenId
  tokenContract {
    collectionAddress
  }
  name
  description
  image {
    url
    mediaEncoding {
      __typename
      ...ImageMediaEncoding
    }
  }
  owner
  mintInfo {
    mintContext {
      blockTimestamp
    }
  }
}
